[pytest]
log_cli = 1
log_cli_level = info
addopts = --capture=no
;👆addopts有这个设定时，log中info标签和print内容顺序排列，无设定时，log中无testcase，print内容，执行成功后可看到